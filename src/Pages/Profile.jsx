
import { AuthContext } from '../Contexts/AuthContext';

const Profile = () => {
    const {user} =use(AuthContext)
    return (
        <div>
            
        </div>
    );
};

export default Profile;